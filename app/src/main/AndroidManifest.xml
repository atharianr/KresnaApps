<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kresnaapps">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KresnaApps">
        <activity android:name=".FunFactActivity"></activity>
        <activity android:name=".LearnQuizActivity" />
        <activity android:name=".LearnSocialActivity" />
        <activity android:name=".LearnMultiplicationActivity" />
        <activity android:name=".LearnSubstractionActivity" />
        <activity android:name=".LearnAdditionActivity" />
        <activity android:name=".SelectDifficultyActivity" />
        <activity android:name=".LearnNumberActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>